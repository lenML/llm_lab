model:
  name: unsloth/Llama-3.2-1B-Instruct
  dtype: auto

training:
  batch_size: 2
  max_seq_length: 2048
  epochs: 1
  learning_rate: 2e-4

data:
  path: ./data/train.jsonl

output:
  dir: ./outputs
  save_steps: 100